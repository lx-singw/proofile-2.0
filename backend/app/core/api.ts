// This TypeScript file was moved to the frontend.
// Please import the browser API client from `frontend/src/lib/api.ts`.
// Keeping a minimal stub here to make accidental imports fail loudly.

export default function backendApiStub(): never {
  throw new Error(
    "Browser API client moved to frontend/src/lib/api.ts. Do not import browser code from the backend."
  );
}
